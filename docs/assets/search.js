window.DOCGEN_SEARCH=[{"title":"Brontie Partner API","path":"index.html","text":"# Brontie Partner API\n\nPartners call this API to issue a Brontie voucher. We return a redemption link. The partner surfaces that link inside their own product. Brontie does not contact the recipient: no email, no SMS. The recipient chooses a partner cafe themselves after receiving the link, not at issue time, so no cafe is associated with a voucher until the recipient picks one. Money comes off a balance topped up in advance. There is no refund path back to the balance, including for vouchers that are never redeemed. Self-service top-up is still being built. Until it ships, balances are credited by arrangement with Brontie. Scope: Republic of Ireland, EUR only. Read the Idempotency section before building. The `idempotencyKey` is required and the retry behaviour around it is the only surprising part of this API.\n"},{"title":"API overview","path":"api/index.html","text":"# API overview\n\nIssue coffee vouchers against a pre-funded balance.\n\n- `https://www.brontie.ie`: Production. Key prefix determines live or test mode.\n\nThis documentation is generated from the [OpenAPI specification](https://github.com/voxgig-sdk/brontie-sdk/blob/main/.sdk/def/brontie.yml) held in the SDK repository.\n\n- [Balance](balance.html)\n- [Voucher](voucher.html)\n"},{"title":"Authentication","path":"guides/authentication.html","text":"# Authentication\n\nConfigure credentials for the scheme described by the API model. Keep credentials outside source control.\n\n\n\n"},{"title":"Make your first API call","path":"guides/first-call.html","text":"# Make your first API call\n\n1. Choose an SDK from the SDK section and follow its installation instructions.\n2. Configure the API server and authentication settings.\n3. Open the [Balance reference](../api/balance.html) and supply the parameters and request body it requires.\n4. Call `load` and inspect the returned entity data.\n\nThe first endpoint is `GET /api/v1/balance`. Request and response examples come from the API specification.\n"},{"title":"Handle errors","path":"guides/errors.html","text":"# Handle errors\n\nCheck the status and error response documented for each operation. Handle authentication and validation failures before retrying a request.\n\nConfigure retry, timeout, and logging through the features present in the selected SDK. Review the feature reference for defaults and options.\n"},{"title":"Entities, SDKs, and tools","path":"guides/concepts.html","text":"# Entities, SDKs, and tools\n\nThe API model defines entities, operations, fields, and endpoints. The API specification supplies request and response details. SDK targets expose those operations in a programming language. Additional targets expose a command interface, an MCP server, or a data integration.\n\nSDKs expose the API operations using each language’s conventions. Read the language reference for configuration and return values.\n"},{"title":"Balance","path":"api/balance.html","text":"# Balance\n\n\n\n## Operations\n\nEvery route this entity exposes. Each route links to its own reference on this page.\n\n| Method | Route | SDK operation | Returns |\n| --- | --- | --- | --- |\n| `GET` | [`/api/v1/balance`](#get-apiv1balance) | `load` | Current balance |\n\n## Examples\n\nOne example per SDK, generated from the API model. The argument names are the parameters and fields of this entity, and the values are placeholders. The examples are not compiled or executed.\n\n### Golang\n\nConstruct `client` as shown in the [Golang setup](../sdks/go.html#set-up-the-client), then call the operations:\n\n\n\n### Lua\n\nConstruct `client` as shown in the [Lua setup](../sdks/lua.html#set-up-the-client), then call the operations:\n\n\n\n### PHP\n\nConstruct `client` as shown in the [PHP setup](../sdks/php.html#set-up-the-client), then call the operations:\n\n\n\n### Python\n\nConstruct `client` as shown in the [Python setup](../sdks/py.html#set-up-the-client), then call the operations:\n\n\n\n### Ruby\n\nConstruct `client` as shown in the [Ruby setup](../sdks/rb.html#set-up-the-client), then call the operations:\n\n\n\n### TypeScript\n\nConstruct `client` as shown in the [TypeScript setup](../sdks/ts.html#set-up-the-client), then call the operations:\n\n\n\n\n## Fields\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `alertAt` | `one,number,null` | Yes | The threshold resolved to a euro figure: `lastTopUp × alertPercent`. |\n| `alertPercent` | `number` | Yes | Percentage of the most recent top-up at which the low-balance threshold sits. |\n| `balance` | `number` | Yes | Balance |\n| `currency` | `string` | Yes | Currency |\n\n## load\n\n\n\n### `GET` /api/v1/balance\n\nOperation ID: `getBalance`.\n\nAuthentication: bearer token (`bearerAuth`).\n\n#### Responses\n\n##### 200: Current balance\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `alertAt` | `number,null` | Yes | The threshold resolved to a euro figure: `lastTopUp × alertPercent`. `null` until the first top-up has been made, because before then there is nothing for the percentage to be a percentage of. |\n| `alertPercent` | `number` | Yes | Percentage of the most recent top-up at which the low-balance threshold sits. Always present; defaults to 20. |\n| `balance` | `number` | Yes |  |\n| `currency` | `string` | Yes |  |\n\n##### 401: Key missing, malformed, revoked or unknown, or a key whose prefix does not match the mode it was issued in.\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `code` | `enum: invalid_json, invalid_product, missing_idempotency_key, invalid_idempotency_key, unauthorized, forbidden, ...` | Yes | Stable machine-readable code. Branch on this, not on `error`. |\n| `error` | `string` | Yes | Human-readable message. Wording may change. |\n\n##### 403: The key is valid but the call is not permitted: either the key lacks the scope for this endpoint (`vouchers:create` or `balance:read`), or the partner account has been deactivated.\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `code` | `enum: invalid_json, invalid_product, missing_idempotency_key, invalid_idempotency_key, unauthorized, forbidden, ...` | Yes | Stable machine-readable code. Branch on this, not on `error`. |\n| `error` | `string` | Yes | Human-readable message. Wording may change. |\n\n##### 429: Rate limited. Honour `Retry-After`. Limits are per key with separate counters for test and live, so a test key cannot exhaust a live budget. Default 120 requests per minute, burst 20 per second, adjustable per partner without a deploy.\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `code` | `enum: invalid_json, invalid_product, missing_idempotency_key, invalid_idempotency_key, unauthorized, forbidden, ...` | Yes | Stable machine-readable code. Branch on this, not on `error`. |\n| `error` | `string` | Yes | Human-readable message. Wording may change. |\n\n"},{"title":"Voucher","path":"api/voucher.html","text":"# Voucher\n\n\n\n## Operations\n\nEvery route this entity exposes. Each route links to its own reference on this page.\n\n| Method | Route | SDK operation | Returns |\n| --- | --- | --- | --- |\n| `POST` | [`/api/v1/vouchers`](#post-apiv1vouchers) | `create` | Idempotent replay. The original voucher for this key, not charged again. # VERIFIED: balanceAfter on a replay is the balance NOW, not the # balance after the original debit. Note that `balanceAfter` in a replay is your balance at the time of the replay, not the balance recorded when the voucher was first issued. If other vouchers have been issued since, it will differ from the 201 you originally received. |\n\n## Examples\n\nOne example per SDK, generated from the API model. The argument names are the parameters and fields of this entity, and the values are placeholders. The examples are not compiled or executed.\n\n### Golang\n\nConstruct `client` as shown in the [Golang setup](../sdks/go.html#set-up-the-client), then call the operations:\n\n\n\n### Lua\n\nConstruct `client` as shown in the [Lua setup](../sdks/lua.html#set-up-the-client), then call the operations:\n\n\n\n### PHP\n\nConstruct `client` as shown in the [PHP setup](../sdks/php.html#set-up-the-client), then call the operations:\n\n\n\n### Python\n\nConstruct `client` as shown in the [Python setup](../sdks/py.html#set-up-the-client), then call the operations:\n\n\n\n### Ruby\n\nConstruct `client` as shown in the [Ruby setup](../sdks/rb.html#set-up-the-client), then call the operations:\n\n\n\n### TypeScript\n\nConstruct `client` as shown in the [TypeScript setup](../sdks/ts.html#set-up-the-client), then call the operations:\n\n\n\n\n## Fields\n\n| Field | Type | Required | Description |\n| --- | --- | --- | --- |\n| `idempotencyKey` | `string` | Yes | Unique per gift on the partner side, scoped per partner and per mode. |\n| `message` | `string` | No | Short personal note shown with the gift. |\n| `product` | `string` | Yes | `coffee` is EUR 5.00, ``coffee_and_cake`` is EUR 10.00. |\n| `recipient` | `object` | No | Recipient |\n| `reference` | `string` | No | Your identifier. |\n| `senderName` | `string` | No | Who the gift appears to be from, per call, so it can vary by course or cohort. |\n\n## create\n\n\n\n### `POST` /api/v1/vouchers\n\nOperation ID: `createVoucher`.\n\nAuthentication: bearer token (`bearerAuth`).\n\n#### Request body\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `idempotencyKey` | `string` | Yes | Unique per gift on the partner side, scoped per partner and per mode. Derive it from your own data, for example learner ID plus milestone. Do not use a timestamp or a UUID generated at call time: a retry would produce a different key, which defeats the purpose. Trimmed before validation: a value that is whitespace-only is rejected as missing, and the 200-character limit applies to the trimmed value. |\n| `message` | `string` | No | Short personal note shown with the gift. Not length limited server-side; keep it short so it displays well. |\n| `product` | `enum: coffee, coffee_and_cake` | Yes | `coffee` is EUR 5.00, ``coffee_and_cake`` is EUR 10.00. Prices are fixed per product, not negotiated per partner. |\n| `recipient` | `object` | No |  |\n| `recipient.email` | `string` | No | Accepted for your records only, stored as sent, the server does not validate that it is a well-formed address. Brontie does not email the recipient. Most partners omit this. |\n| `recipient.name` | `string` | No | Shown on the voucher. Not length limited server-side. |\n| `reference` | `string` | No | Your identifier. Stored and returned unchanged. Not length limited server-side. |\n| `senderName` | `string` | No | Who the gift appears to be from, per call, so it can vary by course or cohort. Falls back to an account default when absent. Brontie cannot construct course names or dates: build the string on your side and send it here. Not length limited server-side. |\n\n#### Responses\n\n##### 200: Idempotent replay. The original voucher for this key, not charged again. # VERIFIED: balanceAfter on a replay is the balance NOW, not the # balance after the original debit. Note that `balanceAfter` in a replay is your balance at the time of the replay, not the balance recorded when the voucher was first issued. If other vouchers have been issued since, it will differ from the 201 you originally received.\n\n`application/json`\n\nThe response is `object and object`.\n\n##### 201: Voucher issued and balance debited\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `amount` | `number` | Yes | Amount in EUR debited from the balance. |\n| `balanceAfter` | `number` | Yes | On a 201, the balance after this debit. For test keys nothing is debited, so this is the untouched balance. On a 200 replay, see the note on that response. |\n| `expiresAt` | `string (date-time)` | Yes | Five years from issue. |\n| `mode` | `enum: live, test` | Yes | Derived from the API key prefix. A `test` voucher is not redeemable and its link opens a page stating so. |\n| `product` | `enum: coffee, coffee_and_cake` | Yes | `coffee` is EUR 5.00, ``coffee_and_cake`` is EUR 10.00. Prices are fixed per product, not negotiated per partner. |\n| `redeemLink` | `string (uri)` | Yes | The only field you need to keep. Surface this to the recipient. Live links are under `/brontie-coffee/`; test links are under `/api-sandbox/voucher/` and open a page stating the voucher cannot be redeemed. |\n| `reference` | `string` | Yes | Always present. Your `reference` echoed back unchanged, or an empty string if you did not supply one. |\n| `voucherToken` | `string` | Yes | Opaque voucher identifier. Test-mode tokens begin with `test_`. |\n\n##### 400: Malformed body or validation failed. Four codes, one per cause.\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `code` | `enum: invalid_json, invalid_product, missing_idempotency_key, invalid_idempotency_key, unauthorized, forbidden, ...` | Yes | Stable machine-readable code. Branch on this, not on `error`. |\n| `error` | `string` | Yes | Human-readable message. Wording may change. |\n\n##### 401: Key missing, malformed, revoked or unknown, or a key whose prefix does not match the mode it was issued in.\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `code` | `enum: invalid_json, invalid_product, missing_idempotency_key, invalid_idempotency_key, unauthorized, forbidden, ...` | Yes | Stable machine-readable code. Branch on this, not on `error`. |\n| `error` | `string` | Yes | Human-readable message. Wording may change. |\n\n##### 402: Insufficient balance. Nothing issued, nothing charged. Live mode only: this response cannot occur for a test key.\n\n`application/json`\n\nThe response is `object and object`.\n\n##### 403: The key is valid but the call is not permitted: either the key lacks the scope for this endpoint (`vouchers:create` or `balance:read`), or the partner account has been deactivated.\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `code` | `enum: invalid_json, invalid_product, missing_idempotency_key, invalid_idempotency_key, unauthorized, forbidden, ...` | Yes | Stable machine-readable code. Branch on this, not on `error`. |\n| `error` | `string` | Yes | Human-readable message. Wording may change. |\n\n##### 409: Two distinct cases, separated by `code`. Branch on the code, not on the status. `issue_in_progress`: a request with this key is genuinely in flight. Retry with backoff and you will receive the original voucher. Occurs in both modes. `issue_stuck`: a claim for this key has been pending for more than five minutes, meaning an earlier attempt died mid-issue. In live mode this is not resolved automatically, because money may already have moved and only a human can reconcile it safely. Brontie is alerted when this happens; contact us rather than retrying in a loop, and do not reuse the key. In test mode a stuck claim self-heals after five minutes and the retry succeeds, so `issue_stuck` is not reproducible with a test key.\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `code` | `enum: invalid_json, invalid_product, missing_idempotency_key, invalid_idempotency_key, unauthorized, forbidden, ...` | Yes | Stable machine-readable code. Branch on this, not on `error`. |\n| `error` | `string` | Yes | Human-readable message. Wording may change. |\n\n##### 429: Rate limited. Honour `Retry-After`. Limits are per key with separate counters for test and live, so a test key cannot exhaust a live budget. Default 120 requests per minute, burst 20 per second, adjustable per partner without a deploy.\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `code` | `enum: invalid_json, invalid_product, missing_idempotency_key, invalid_idempotency_key, unauthorized, forbidden, ...` | Yes | Stable machine-readable code. Branch on this, not on `error`. |\n| `error` | `string` | Yes | Human-readable message. Wording may change. |\n\n##### 500: Internal error. Always this exact shape, `&#123; error, code &#125;`, even for a fault deep inside the request. A top-level guard on this route (added after the rest of this spec was first verified) catches anything that would otherwise have escaped as a bare platform error with no `code` to branch on. For the ordinary case, something failed before any state was touched, or during the mint step itself, any partial work is rolled back before responding: a debit is refunded and the claim released, so retrying with the same key is safe and will succeed. One case is narrower: if the fault happens while this route is already handling an abandoned claim from an earlier attempt (see `issue_stuck` above), nothing is rolled back on purpose, a debit from that earlier attempt may or may not have gone through, and guessing wrong would either double-charge or lose track of a voucher. &quot;Safe to retry&quot; still holds in the sense that matters: the same key can never mint twice. It does not guarantee the retry succeeds, in this specific case it will most likely come back as `409 issue_stuck` rather than a fresh `201`, which is the correct outcome, not a bug to work around.\n\n`application/json`\n\n| Property | Type | Required | Description |\n| --- | --- | --- | --- |\n| `code` | `enum: invalid_json, invalid_product, missing_idempotency_key, invalid_idempotency_key, unauthorized, forbidden, ...` | Yes | Stable machine-readable code. Branch on this, not on `error`. |\n| `error` | `string` | Yes | Human-readable message. Wording may change. |\n\n"},{"title":"Golang","path":"sdks/go.html","text":"# Golang\n\nGolang\n\n## Install\n\nNot published. Build from the go directory.\n\n## Package\n\n`github.com/voxgig-sdk/brontie-sdk/go`\n\n## API reference\n\n| Entity | Operations |\n| --- | --- |\n| [Balance](../api/balance.html) | `load` |\n| [Voucher](../api/voucher.html) | `create` |\n\n## Configuration\n\nSet the server URL and credentials for your environment. Enable only the features your application needs.\n\n## Features\n\n- [Request/response capture ring buffer for debugging](../features/debug.html)\n- [Idempotency keys for safe retries of mutating operations](../features/idempotency.html)\n- [Statistics capture: per-operation counters and latency](../features/metrics.html)\n- [Pagination signals for list operations](../features/paging.html)\n- [Client-side rate limiting via a token bucket](../features/ratelimit.html)\n- [Automatic retry of transient failures with exponential backoff](../features/retry.html)\n- [In-memory mock transport for testing without a live server](../features/test.html)\n- [Per-request timeout with transport abort](../features/timeout.html)\n\n## Set up the client\n\nThis setup fragment reads credentials from the environment. Use the API operation reference to supply input for each call.\n\n\n\n\n\n## Client defaults\n\n\n\n\n"},{"title":"Go CLI","path":"tools/go-cli.html","text":"# Go CLI\n\nGo CLI\n\n## Install\n\nNot published. Build from the go-cli directory.\n\n## Package\n\n`github.com/voxgig-sdk/brontie-sdk/go-cli`\n\n## API reference\n\n| Entity | Operations |\n| --- | --- |\n| [Balance](../api/balance.html) | `load` |\n| [Voucher](../api/voucher.html) | `create` |\n\n## Configuration\n\nSet the server URL and credentials for your environment. Enable only the features your application needs.\n\n## Features\n\n- [Request/response capture ring buffer for debugging](../features/debug.html)\n- [Idempotency keys for safe retries of mutating operations](../features/idempotency.html)\n- [Statistics capture: per-operation counters and latency](../features/metrics.html)\n- [Pagination signals for list operations](../features/paging.html)\n- [Client-side rate limiting via a token bucket](../features/ratelimit.html)\n- [Automatic retry of transient failures with exponential backoff](../features/retry.html)\n- [In-memory mock transport for testing without a live server](../features/test.html)\n- [Per-request timeout with transport abort](../features/timeout.html)\n"},{"title":"Go MCP server","path":"tools/go-mcp.html","text":"# Go MCP server\n\nGo MCP server\n\n## Install\n\nNot published. Build from the go-mcp directory.\n\n## Package\n\n`github.com/voxgig-sdk/brontie-sdk/go-mcp`\n\n## API reference\n\nThe API reference covers all entities. The tools section lists the operations this server exposes.\n\n| Entity | Operations |\n| --- | --- |\n| [Balance](../api/balance.html) | `load` |\n| [Voucher](../api/voucher.html) | `create` |\n\n## Configuration\n\nSet the server URL and credentials for your environment. Enable only the features your application needs.\n\n## Features\n\n- [Request/response capture ring buffer for debugging](../features/debug.html)\n- [Idempotency keys for safe retries of mutating operations](../features/idempotency.html)\n- [Statistics capture: per-operation counters and latency](../features/metrics.html)\n- [Pagination signals for list operations](../features/paging.html)\n- [Client-side rate limiting via a token bucket](../features/ratelimit.html)\n- [Automatic retry of transient failures with exponential backoff](../features/retry.html)\n- [In-memory mock transport for testing without a live server](../features/test.html)\n- [Per-request timeout with transport abort](../features/timeout.html)\n\n## Tools\n\n### `brontie_list`\n\nList records for an entity.\n\n\n\nThis tool is registered, but no active entity supports `list`.\n\n### `brontie_load`\n\nLoad one record for an entity.\n\n\n\nSupported entities: `balance`.\n\n"},{"title":"Lua","path":"sdks/lua.html","text":"# Lua\n\nLua\n\n## Install\n\nNot published. Build from the lua directory.\n\n## Package\n\n`voxgig-sdk-brontie`\n\n## API reference\n\n| Entity | Operations |\n| --- | --- |\n| [Balance](../api/balance.html) | `load` |\n| [Voucher](../api/voucher.html) | `create` |\n\n## Configuration\n\nSet the server URL and credentials for your environment. Enable only the features your application needs.\n\n## Features\n\n- [Request/response capture ring buffer for debugging](../features/debug.html)\n- [Idempotency keys for safe retries of mutating operations](../features/idempotency.html)\n- [Statistics capture: per-operation counters and latency](../features/metrics.html)\n- [Pagination signals for list operations](../features/paging.html)\n- [Client-side rate limiting via a token bucket](../features/ratelimit.html)\n- [Automatic retry of transient failures with exponential backoff](../features/retry.html)\n- [In-memory mock transport for testing without a live server](../features/test.html)\n- [Per-request timeout with transport abort](../features/timeout.html)\n\n## Set up the client\n\nThis setup fragment reads credentials from the environment. Use the API operation reference to supply input for each call.\n\n\n\n\n\n## Client defaults\n\n\n\n\n"},{"title":"PHP","path":"sdks/php.html","text":"# PHP\n\nPHP\n\n## Install\n\nNot published. Build from the php directory.\n\n## Package\n\n`voxgig-sdk/brontie`\n\n## API reference\n\n| Entity | Operations |\n| --- | --- |\n| [Balance](../api/balance.html) | `load` |\n| [Voucher](../api/voucher.html) | `create` |\n\n## Configuration\n\nSet the server URL and credentials for your environment. Enable only the features your application needs.\n\n## Features\n\n- [Request/response capture ring buffer for debugging](../features/debug.html)\n- [Idempotency keys for safe retries of mutating operations](../features/idempotency.html)\n- [Statistics capture: per-operation counters and latency](../features/metrics.html)\n- [Pagination signals for list operations](../features/paging.html)\n- [Client-side rate limiting via a token bucket](../features/ratelimit.html)\n- [Automatic retry of transient failures with exponential backoff](../features/retry.html)\n- [In-memory mock transport for testing without a live server](../features/test.html)\n- [Per-request timeout with transport abort](../features/timeout.html)\n\n## Set up the client\n\nThis setup fragment reads credentials from the environment. Use the API operation reference to supply input for each call.\n\n\n\n\n\n## Client defaults\n\n\n\n\n"},{"title":"Python","path":"sdks/py.html","text":"# Python\n\nPython\n\n## Install\n\nNot published. Build from the py directory.\n\n## Package\n\n`voxgig-sdk-brontie`\n\n## API reference\n\n| Entity | Operations |\n| --- | --- |\n| [Balance](../api/balance.html) | `load` |\n| [Voucher](../api/voucher.html) | `create` |\n\n## Configuration\n\nSet the server URL and credentials for your environment. Enable only the features your application needs.\n\n## Features\n\n- [Request/response capture ring buffer for debugging](../features/debug.html)\n- [Idempotency keys for safe retries of mutating operations](../features/idempotency.html)\n- [Statistics capture: per-operation counters and latency](../features/metrics.html)\n- [Pagination signals for list operations](../features/paging.html)\n- [Client-side rate limiting via a token bucket](../features/ratelimit.html)\n- [Automatic retry of transient failures with exponential backoff](../features/retry.html)\n- [In-memory mock transport for testing without a live server](../features/test.html)\n- [Per-request timeout with transport abort](../features/timeout.html)\n\n## Set up the client\n\nThis setup fragment reads credentials from the environment. Use the API operation reference to supply input for each call.\n\n\n\n\n\n## Client defaults\n\n\n\n\n"},{"title":"Ruby","path":"sdks/rb.html","text":"# Ruby\n\nRuby\n\n## Install\n\nNot published. Build from the rb directory.\n\n## Package\n\n`voxgig-sdk-brontie`\n\n## API reference\n\n| Entity | Operations |\n| --- | --- |\n| [Balance](../api/balance.html) | `load` |\n| [Voucher](../api/voucher.html) | `create` |\n\n## Configuration\n\nSet the server URL and credentials for your environment. Enable only the features your application needs.\n\n## Features\n\n- [Request/response capture ring buffer for debugging](../features/debug.html)\n- [Idempotency keys for safe retries of mutating operations](../features/idempotency.html)\n- [Statistics capture: per-operation counters and latency](../features/metrics.html)\n- [Pagination signals for list operations](../features/paging.html)\n- [Client-side rate limiting via a token bucket](../features/ratelimit.html)\n- [Automatic retry of transient failures with exponential backoff](../features/retry.html)\n- [In-memory mock transport for testing without a live server](../features/test.html)\n- [Per-request timeout with transport abort](../features/timeout.html)\n\n## Set up the client\n\nThis setup fragment reads credentials from the environment. Use the API operation reference to supply input for each call.\n\n\n\n\n\n## Client defaults\n\n\n\n\n"},{"title":"TypeScript","path":"sdks/ts.html","text":"# TypeScript\n\nTypeScript\n\n## Install\n\nNot published. Build from the ts directory.\n\n## Package\n\n`@voxgig-sdk/brontie`\n\n## API reference\n\n| Entity | Operations |\n| --- | --- |\n| [Balance](../api/balance.html) | `load` |\n| [Voucher](../api/voucher.html) | `create` |\n\n## Configuration\n\nSet the server URL and credentials for your environment. Enable only the features your application needs.\n\n## Features\n\n- [Request/response capture ring buffer for debugging](../features/debug.html)\n- [Idempotency keys for safe retries of mutating operations](../features/idempotency.html)\n- [Statistics capture: per-operation counters and latency](../features/metrics.html)\n- [Pagination signals for list operations](../features/paging.html)\n- [Client-side rate limiting via a token bucket](../features/ratelimit.html)\n- [Automatic retry of transient failures with exponential backoff](../features/retry.html)\n- [In-memory mock transport for testing without a live server](../features/test.html)\n- [Per-request timeout with transport abort](../features/timeout.html)\n\n## Set up the client\n\nThis setup fragment reads credentials from the environment. Use the API operation reference to supply input for each call.\n\n\n\n\n\n## Client defaults\n\n\n\n\n"},{"title":"Request/response capture ring buffer for debugging","path":"features/debug.html","text":"# Request/response capture ring buffer for debugging\n\n\n\nA feature adds behaviour around API calls without changing how you call the API. Being documented here does not mean it is switched on: features are off by default, and you enable the ones your application needs when you construct the client.\n\n## Options\n\nThe settings this feature reads, with the defaults compiled into the SDK. Pass replacements in the client configuration to change them; set `active` to turn the feature on.\n\n\n\n\n\n## Pipeline stages\n\nThe points in the request lifecycle where this feature runs. The SDK calls each stage in order as it prepares a request, sends it, and handles the response.\n\n- `PreDone`\n- `PreRequest`\n- `PreResponse`\n- `PreUnexpected`\n"},{"title":"Idempotency keys for safe retries of mutating operations","path":"features/idempotency.html","text":"# Idempotency keys for safe retries of mutating operations\n\n\n\nA feature adds behaviour around API calls without changing how you call the API. Being documented here does not mean it is switched on: features are off by default, and you enable the ones your application needs when you construct the client.\n\n## Options\n\nThe settings this feature reads, with the defaults compiled into the SDK. Pass replacements in the client configuration to change them; set `active` to turn the feature on.\n\n\n\n\n\n## Pipeline stages\n\nThe points in the request lifecycle where this feature runs. The SDK calls each stage in order as it prepares a request, sends it, and handles the response.\n\n- `PreRequest`\n"},{"title":"Statistics capture: per-operation counters and latency","path":"features/metrics.html","text":"# Statistics capture: per-operation counters and latency\n\n\n\nA feature adds behaviour around API calls without changing how you call the API. Being documented here does not mean it is switched on: features are off by default, and you enable the ones your application needs when you construct the client.\n\n## Options\n\nThe settings this feature reads, with the defaults compiled into the SDK. Pass replacements in the client configuration to change them; set `active` to turn the feature on.\n\n\n\n\n\n## Pipeline stages\n\nThe points in the request lifecycle where this feature runs. The SDK calls each stage in order as it prepares a request, sends it, and handles the response.\n\n- `PreDone`\n- `PrePoint`\n- `PreUnexpected`\n"},{"title":"Pagination signals for list operations","path":"features/paging.html","text":"# Pagination signals for list operations\n\n\n\nA feature adds behaviour around API calls without changing how you call the API. Being documented here does not mean it is switched on: features are off by default, and you enable the ones your application needs when you construct the client.\n\n## Options\n\nThe settings this feature reads, with the defaults compiled into the SDK. Pass replacements in the client configuration to change them; set `active` to turn the feature on.\n\n\n\n\n\n## Pipeline stages\n\nThe points in the request lifecycle where this feature runs. The SDK calls each stage in order as it prepares a request, sends it, and handles the response.\n\n- `PreRequest`\n- `PreResult`\n"},{"title":"Client-side rate limiting via a token bucket","path":"features/ratelimit.html","text":"# Client-side rate limiting via a token bucket\n\n\n\nA feature adds behaviour around API calls without changing how you call the API. Being documented here does not mean it is switched on: features are off by default, and you enable the ones your application needs when you construct the client.\n\n## Options\n\nThe settings this feature reads, with the defaults compiled into the SDK. Pass replacements in the client configuration to change them; set `active` to turn the feature on.\n\n\n\n\n\n## Pipeline stages\n\nNo pipeline stages are enabled for this feature.\n"},{"title":"Automatic retry of transient failures with exponential backoff","path":"features/retry.html","text":"# Automatic retry of transient failures with exponential backoff\n\n\n\nA feature adds behaviour around API calls without changing how you call the API. Being documented here does not mean it is switched on: features are off by default, and you enable the ones your application needs when you construct the client.\n\n## Options\n\nThe settings this feature reads, with the defaults compiled into the SDK. Pass replacements in the client configuration to change them; set `active` to turn the feature on.\n\n\n\n\n\n## Pipeline stages\n\nNo pipeline stages are enabled for this feature.\n"},{"title":"In-memory mock transport for testing without a live server","path":"features/test.html","text":"# In-memory mock transport for testing without a live server\n\n\n\nA feature adds behaviour around API calls without changing how you call the API. Being documented here does not mean it is switched on: features are off by default, and you enable the ones your application needs when you construct the client.\n\n## Options\n\nThe settings this feature reads, with the defaults compiled into the SDK. Pass replacements in the client configuration to change them; set `active` to turn the feature on.\n\n\n\n\n\n## Pipeline stages\n\nThe points in the request lifecycle where this feature runs. The SDK calls each stage in order as it prepares a request, sends it, and handles the response.\n\n- `GetData`\n- `GetMatch`\n- `PostConstruct`\n- `PostConstructEntity`\n- `PrePoint`\n- `PreRequest`\n- `PreResponse`\n- `PreResult`\n- `PreSpec`\n- `SetData`\n- `SetMatch`\n"},{"title":"Per-request timeout with transport abort","path":"features/timeout.html","text":"# Per-request timeout with transport abort\n\n\n\nA feature adds behaviour around API calls without changing how you call the API. Being documented here does not mean it is switched on: features are off by default, and you enable the ones your application needs when you construct the client.\n\n## Options\n\nThe settings this feature reads, with the defaults compiled into the SDK. Pass replacements in the client configuration to change them; set `active` to turn the feature on.\n\n\n\n\n\n## Pipeline stages\n\nNo pipeline stages are enabled for this feature.\n"}];
const search=document.getElementById('search'),results=document.getElementById('results');
search.addEventListener('input',()=>{results.replaceChildren();const q=search.value.trim().toLowerCase();if(q.length<2)return;for(const page of window.DOCGEN_SEARCH.filter(p=>(p.title+' '+p.text).toLowerCase().includes(q)).slice(0,12)){const a=document.createElement('a');a.href=search.dataset.base+page.path;a.textContent=page.title;results.append(a)}});
